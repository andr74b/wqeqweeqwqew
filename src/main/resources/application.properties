spring.application.name=blackjack
spring.threads.virtual.enabled=true
server.port=${PORT:8080}
vaadin.allowed-packages=org.example
# Production is the default, including launches from an IDE or spring-boot:run.
# The Maven development profile opts out only for its local JVM.
vaadin.productionMode=true
